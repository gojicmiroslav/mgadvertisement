require "rails_helper"

describe 'Admin changes advertisement status' do
  # fixtures :admin
  # fixtures :advertisement_types
  # fixtures :users
  # fixtures :information_types
  # fixtures :information
  # fixtures :items
  # fixtures :categories
  # fixtures :vehicle_brands
  # fixtures :vehicle_models
  # fixtures :options
  # fixtures :advertisements

  scenario "admin changes advertisement status and app sends email to user" do
    #admin_login(admin(:admin_one).email, "password")
    #visit "/admin/advertisements/#{advertisements(:pending_advertisement).id}/edit"
    #visit edit_admin_advertisement_path(advertisements(:pending_advertisement).id)
    #expect(page).to have_content("Edit Advertisement")
  end
end
